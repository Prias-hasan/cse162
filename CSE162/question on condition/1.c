// Write a program in C that takes three inputs a, b, and c from user and prints them in ascending order.
#include<stdio.h>
int main(){
    int a,b,c;
    printf("give the nummber a:");
    scanf("%d",&a);
    printf("give the nummber b:");
    scanf("%d",&b);
    printf("give the nummber c:");
    scanf("%d",&c);

    if((a>b)&&(a>c)){

    printf("%d",a);
    
        elseif()
    }
}